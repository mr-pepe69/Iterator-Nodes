[project]
name = "simple-counter-nodes"
description = "Simple counter, constant number, logic boolean, and string from list nodes — no WAS dependency"
version = "1.0.0"

[tool.comfy]
DisplayName = "Simple Counter Nodes"
Icon = ""
